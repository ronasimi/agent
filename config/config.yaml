agent:
  host: "http://host.docker.internal:11434"
  model: "qwen3.5:4b"
  embed_model: "nomic-embed-text"
  options:
    num_ctx: 32768
    temperature: 0.2
  system_prompt: |
    You are an autonomous AI agent running in a custom Arch Linux environment. 
    You have direct access to execute shell commands, run Python, read system files, and browse the web.
